package co

type Colors struct {
<% colors.forEach(function (color) { %> <%= color.name -%>     ColorFunc
<% }) %>
}

func noop(s string) string {
	return s
}

var noColors = Colors{
<% colors.forEach(function (color) { %><%= color.name %>:noop,
<% }) %>
}

var colors = Colors{
<% colors.forEach(function (color) { %> <%= color.name %>,
<% }) %>
}
<% colors.forEach(function (color) { %>
var <%= color.name %> = build<% if (color.replace) { %>WithReplace<% } %>(<%= color.open %>, <%= color.close %><% if (color.replace) { %>, <%- color.replace %><% } %>)<% }) %>
    